.message-area{data: {message: {id: message.id}}}
  .message-area__user-status
    .message-area__user-status__name
      = message.user.name
    .message-area__user-status__timestamp
      = message.created_at.strftime("%Y年%m月%d日 %H時%M分")
  .message-area__message
    - if message.body.present?
      %p.message-area__message__body
        = message.body
    = image_tag message.image.url, class: 'message-area__message__image' if message.image.present?